<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html>
  <head>
    <title>synthlijst.nl</title>
    <%= stylesheet_link_tag :all, :cache => true %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <div id="user_nav">
        <% if current_user %>
          <%= link_to "Eigen profile aanpassen", edit_user_path(:current) %> |
          <%= link_to "Uitloggen", logout_path %>
        <% else %>
          <%= link_to "Registreer", new_user_path %> |
          <%= link_to "InLoggen", login_path %>
        <% end -%>
      </div>
      
      <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end -%>
      
      <%- if show_title? -%>
        <h1><%=h yield(:title) %></h1>
      <% end -%>
      
      <%= yield %>
    </div>
  </body>
</html>